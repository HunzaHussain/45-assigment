"use strict";
// ASSINGMENT(QUESTION 6)
//* Stripping Names: Store a Person's name and include some whitespace character at the beginning and end of the name.
//  Make sure you use each character combination."\t"and"\n".
//  at least once.Print the name once,so the whitespace around the name is displayed. then print the name after striping the whitespaces.//*
let myName = "\t\n Hunza Hussain \t\n"; // This saves the name with whitespace
console.log("Name with whitespace :", myName); // Shows the name with whitespace
console.log("Name without whitespace :", myName.trim()); // Shows the name without whitespace
